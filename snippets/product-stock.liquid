<ap-inventoryproduct form-id="ap-productform-{{ section.id }}-{{ product.id }}" class="ap-productform__inventory-wrapper">
    <span class="inventory inventory--high">
        {% if product.variants[0]["available"] %}
            <span class="inventory_stock">
                {{ product.selected_or_first_available_variant.inventory_quantity }}
            </span>

        {% else %}
            <span class="inventory_outstock">
                {{ product.selected_or_first_available_variant.inventory_quantity }}
            </span>
        {% endif %}
    </span>
    <script type="application/json">
        {
            {%- assign _size = product.variants | size -%}
            {%- for variant in product.variants -%}
                {%- if product.variants[0]["available"] -%}
                    "{{- variant.id -}}": "<span class=\"inventory inventory--high\">{{ product.selected_or_first_available_variant.inventory_quantity }}</span>"
                {%- else -%}
                    "{{- variant.id -}}": "<span class=\"inventory inventory--high\">{{ product.selected_or_first_available_variant.inventory_quantity }}</span>"
                {%- endif -%}
                {%- if forloop.index < _size and _size > 1 -%},{%- endif -%}
            {%- endfor -%}
        }
    </script>
</ap-inventoryproduct>
